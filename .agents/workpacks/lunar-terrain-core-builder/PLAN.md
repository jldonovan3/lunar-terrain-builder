# LunarTerrainCore / LunarTerrainBuilder workpack

Authoritative scope and milestone definitions: [`plans/01_LunarTerrainCore_LunarTerrainBuilder_Implementation_Plan.md`](../../../plans/01_LunarTerrainCore_LunarTerrainBuilder_Implementation_Plan.md).

Architecture: [`specs/01_LunarTerrainBuilder_Architecture_v0.3.md`](../../../specs/01_LunarTerrainBuilder_Architecture_v0.3.md).

Current milestone: M8 spike — Work Stages 1–3 are implemented and pass Windows/MSVC debug and release verification. Product preflight, acquisition locks, portable provisioning profiles, real Profile A–C and scale configurations, lock-consistency enforcement, logical multi-file raster cataloging, and pre-build source/coverage/policy/hierarchy diagnostics are complete. Work Stage 4 has not started.

Blockers: None for Work Stages 1–3. The required LOLA foundation, full SLDEM2015 set, Maskelyne NAC bundle, and GSFC south-polar bundle are provisioned outside the repository and pass strict no-network verification against [`provisioning/provisioning.json`](../../../provisioning/provisioning.json). Stage 4 scientific/pipeline qualification remains intentionally unexecuted, so the configured fusion and polar effective-resolution treatments remain starting policies rather than accepted results.

Verification:

- M2 acceptance is complete, including its Linux ASan/UBSan workflow.
- M3 passed the Windows/MSVC debug and release workflows, generated-raster catalog/build/Core-roundtrip tests, explicit datum and no-data tests, and the pinned-SLDEM2015 acceptance workflow.
- M4 passed the Linux debug, release, and ASan/UBSan workflows (45/45 tests each), including deterministic seam/corner ownership, post-seam quantization, materialized and virtual aprons, and dependency-named binary64 staging.
- M5 passed the Windows/MSVC debug and release workflows (50/50 tests each) and Linux sanitizer verification, including priority/DatasetID ordering independent of configuration order; `Replace`, `BiasCorrectedReplace`, and `ResidualRefinement_v1`; the versioned binomial filter, pass-count, normalized no-data, and 32-sample smoothstep rules; broad-geometry/residual checks; continuous transition derivatives; finalized byte-identical tile edges; multi-dataset LTDB/Core round trips; sorted provenance palettes and 64×64 dominant-source/quality maps; deterministic reordered-source builds; diagnostic PPM/CSV exports; and the pinned SLDEM2015 round trip.
- M6 passed the Linux debug and release workflows (60/60 tests each) and the Linux ASan/UBSan workflow (60/60 tests with LeakSanitizer disabled for the managed ptrace environment). Acceptance covers worst-case effective-resolution level selection; connected sparse L8/L10/L12 hierarchy and child masks; nearest-ancestor bilinear-U16 geometric error; SQLite dependency/content caching; dependent-tile-only invalidation; staged-tile reuse; canonical deterministic repacking; clean/incremental byte identity; bounded tasks; cancellation before publication; and interrupted-publication recovery. The pinned SLDEM2015 round trip and frozen v1 fixture checker passed, and the format fixtures and Core dependency boundary remain unchanged.
- M7 passed the Windows/MSVC debug and release workflows (67/67 tests each), including complete structural/hash/CRC, projection, scientific, seam, provenance, quality, and hierarchy validation; expanded inspection; classified dataset/dependency/content/provenance/package diffing; PLY, OBJ, 16-bit diagnostic PGM, sample CSV, raw little-endian U16, provenance, quality, and transition exports; operator CLI integration; corruption and interrupted-publication coverage; and deterministic clean/incremental benchmark reporting. Controlled six-face synthetic and hash-pinned SLDEM2015 benchmark records are recorded under [`benchmarks/m7`](../../../benchmarks/m7/README.md); both achieved full incremental reuse and identical published identities. The pinned SLDEM2015 acceptance test and frozen v1 fixture checker passed, and the v1 format fixtures, 1 GiB pack target, Zstandard level 3, 64×64 auxiliary maps, fusion semantics, and Core dependency boundary remain unchanged. M7 Linux debug, release, and ASan/UBSan verification has not yet run in this Windows environment.
- M8 Work Stages 1–3 passed the Windows/MSVC debug and release workflows (70/70 tests each). Coverage includes the standard-library provisioning suite, complete SLDEM full-set metadata and canonical identity, the new LOLA/Maskelyne/south-polar product locks and profiles, network-free Builder-lock consistency, semantic required regions and logical multi-file raster bundles, NaN/default metadata handling, full-longitude and pole-containing source footprints, preserved M0–M7 acceptance, and expanded source/hierarchy reporting.
    - Strict `--verify-only` checks passed against all four external roots. Locked original-member totals are 1,848,004,754 bytes for LOLA, 5,466,757,104 bytes for SLDEM, 136,882,682 bytes for Maskelyne, and 11,915,218,975 bytes for the south-polar bundle. The established SLDEM three-member artifact identity remains unchanged.
    - Builder-linked GDAL catalog and plan checks passed for Profiles A–C and scale. The observed source levels/hierarchy totals are A: L8/L12 and 2,480 tiles; B: L7/L8 and 41 tiles; C: L7/L8 and 77 tiles; scale: L7/L8/L12 and 481,225 tiles. Full evidence, source identities, semantic hashes, spot checks, starting policies, quality limitations, and portable commands are in [`qualification/m8/README.md`](../../../qualification/m8/README.md).
    - M8 Linux debug, release, and ASan/UBSan verification remains pending. No Stage 4 database build, full validation, deterministic rebuild, reorder test, incremental invalidation test, or publication evidence has been produced.

Next action: Pause at the requested boundary. Begin Work Stage 4 required-region pipeline verification only with explicit authorization; then run Profiles A–C through the complete operator path and perform the required Windows/Linux determinism and incremental checks. Linux verification of the accumulated M7/M8 changes remains pending.
