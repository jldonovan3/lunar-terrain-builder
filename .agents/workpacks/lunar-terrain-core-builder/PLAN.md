# LunarTerrainCore / LunarTerrainBuilder workpack

Authoritative scope and milestone definitions: [`plans/01_LunarTerrainCore_LunarTerrainBuilder_Implementation_Plan.md`](../../../plans/01_LunarTerrainCore_LunarTerrainBuilder_Implementation_Plan.md).

Architecture: [`specs/01_LunarTerrainBuilder_Architecture_v0.3.md`](../../../specs/01_LunarTerrainBuilder_Architecture_v0.3.md).

Current milestone: M8 run optimization — the corrected Preparation contracts and Priority 0 implementation and real-data exit gate are complete on Windows/MSVC; Priority 1 has not begun. Alternative A now governs invalidation acceptance, and the eventual SLDEM2015 acquisition contract is one locked 96-member full-set identity with regional bounds supplied only by Builder configuration. Builder commands now have versioned telemetry, explicit machine-local budgets, cooperative cancellation, stable exit codes, atomic partial reports, a detached observer, resumable qualification orchestration, provisioning byte/rate progress, and a locked scale-probe definition. The first large scale attempt failed or terminated incompletely; current whole-level staging is not resource-bounded, and no scale benchmark is claimed to be running.

Blockers: None for completed Work Stages 1–3, Preparation, or Priority 0. Current whole-level staging and full-raster decoded caches remain scale blockers to be addressed only in Priority 1: the real probe retained 31,279,479,900 decoded-cache bytes against the observational 8,192 MiB sub-budget and reached 31,642,853,376 bytes peak RSS, although it stayed below the 48 GiB process ceiling. Linux qualification also requires a Linux host; WSL is not installed. The former frozen-v1 invalidation conflict is resolved by Alternative A without changing v1: scientific content/provenance changes are footprint-confined, global dependency/database/package changes are permitted, and restoration must converge exactly.

Verification:

- M2 acceptance is complete, including its Linux ASan/UBSan workflow.
- M3 passed the Windows/MSVC debug and release workflows, generated-raster catalog/build/Core-roundtrip tests, explicit datum and no-data tests, and the pinned-SLDEM2015 acceptance workflow.
- M4 passed the Linux debug, release, and ASan/UBSan workflows (45/45 tests each), including deterministic seam/corner ownership, post-seam quantization, materialized and virtual aprons, and dependency-named binary64 staging.
- M5 passed the Windows/MSVC debug and release workflows (50/50 tests each) and Linux sanitizer verification, including priority/DatasetID ordering independent of configuration order; `Replace`, `BiasCorrectedReplace`, and `ResidualRefinement_v1`; the versioned binomial filter, pass-count, normalized no-data, and 32-sample smoothstep rules; broad-geometry/residual checks; continuous transition derivatives; finalized byte-identical tile edges; multi-dataset LTDB/Core round trips; sorted provenance palettes and 64×64 dominant-source/quality maps; deterministic reordered-source builds; diagnostic PPM/CSV exports; and the pinned SLDEM2015 round trip.
- M6 passed the Linux debug and release workflows (60/60 tests each) and the Linux ASan/UBSan workflow (60/60 tests with LeakSanitizer disabled for the managed ptrace environment). Acceptance covers worst-case effective-resolution level selection; connected sparse L8/L10/L12 hierarchy and child masks; nearest-ancestor bilinear-U16 geometric error; SQLite dependency/content caching; dependent-tile-only invalidation; staged-tile reuse; canonical deterministic repacking; clean/incremental byte identity; bounded tasks; cancellation before publication; and interrupted-publication recovery. The pinned SLDEM2015 round trip and frozen v1 fixture checker passed, and the format fixtures and Core dependency boundary remain unchanged.
- M7 passed the Windows/MSVC debug and release workflows (67/67 tests each), including complete structural/hash/CRC, projection, scientific, seam, provenance, quality, and hierarchy validation; expanded inspection; classified dataset/dependency/content/provenance/package diffing; PLY, OBJ, 16-bit diagnostic PGM, sample CSV, raw little-endian U16, provenance, quality, and transition exports; operator CLI integration; corruption and interrupted-publication coverage; and deterministic clean/incremental benchmark reporting. Controlled six-face synthetic and hash-pinned SLDEM2015 benchmark records are recorded under [`benchmarks/m7`](../../../benchmarks/m7/README.md); both achieved full incremental reuse and identical published identities. The pinned SLDEM2015 acceptance test and frozen v1 fixture checker passed, and the v1 format fixtures, 1 GiB pack target, Zstandard level 3, 64×64 auxiliary maps, fusion semantics, and Core dependency boundary remain unchanged. M7 Linux debug, release, and ASan/UBSan verification has not yet run in this Windows environment.
- M8 Work Stages 1–3 passed the Windows/MSVC debug and release workflows (70/70 tests each). Coverage includes the standard-library provisioning suite, complete SLDEM full-set metadata and canonical identity, the new LOLA/Maskelyne/south-polar product locks and profiles, network-free Builder-lock consistency, semantic required regions and logical multi-file raster bundles, NaN/default metadata handling, full-longitude and pole-containing source footprints, preserved M0–M7 acceptance, and expanded source/hierarchy reporting.
    - Strict `--verify-only` checks passed against all four external roots. Locked original-member totals are 1,848,004,754 bytes for LOLA, 5,466,757,104 bytes for SLDEM, 136,882,682 bytes for Maskelyne, and 11,915,218,975 bytes for the south-polar bundle. The established SLDEM three-member artifact identity remains unchanged.
    - Builder-linked GDAL catalog and plan checks passed for Profiles A–C and scale. The observed source levels/hierarchy totals are A: L8/L12 and 2,480 tiles; B: L7/L8 and 41 tiles; C: L7/L8 and 77 tiles; scale: L7/L8/L12 and 481,225 tiles. Full evidence, source identities, semantic hashes, spot checks, starting policies, quality limitations, and portable commands are in [`qualification/m8/README.md`](../../../qualification/m8/README.md).
    - The complete-hierarchy and decoded-source-cache additions pass the final Windows/MSVC debug and release suites (73/73 each), including a 30-tile generated hierarchy, reversed declarations, full validation, unchanged incremental reuse, explicit little-endian decoded cache reuse, cache-corruption rejection, and explicit prototype-tile diagnostics. This does not establish bounded scale behavior. The complete Profile A–C matrices passed. Their clean/reversed/restored database identities respectively converge to A `52ce12f6d19523aa2e388a6cc81d5e5c177050b5da9d84abdfab086d01865d09` (2,480 tiles, 26 packs, 4,479 seams), B `3cf457349f07b123e70a11e9e836f0e88f4e3da975a304fc4e23faea4ec63ae4` (41 tiles, nine packs, 29 seams), and C `4ee43670c0a74539e4b472af15bfe4ff4e3ded91be930bc6607cbd1be3c7fc7f` (77 tiles, nine packs, 108 seams). Every clean/reordered/restored identity and ordered pack list is byte-identical; full validation, inspect, diff, quality/provenance/transition exports, and policy variants passed. Removing each refinement rebuilt all remaining tiles with zero reuse; restoration reused only the removed refinement's untouched cache rows and converged exactly.
    - M8 Linux debug, release, and ASan/UBSan verification remains pending because the current host has no Linux or WSL environment.
- Run-optimization Preparation and Priority 0 pass the Windows/MSVC debug and release workflows (80/80 tests each), and the frozen format-v1 fixture checker passes unchanged. Coverage includes monotonic progress-v1 events and resource counters, periodic human progress, atomic report/state replacement under concurrent Windows readers, benchmark-v2 phase/checkpoint/identity evidence, qualification-v2 resume manifests and streamed logs, provisioning member byte/rate progress, CLI usage/operational/cancellation exit codes, and the generic observer lifecycle. The self-contained cancellation gate stops after a synced scan checkpoint with exit 130, starts a distinct resumed process, and reproduces all seven published synthetic files byte-for-byte against a fresh build.
    - Strict no-network verification passes against all four configured real-data roots. The detached probe used the complete four-source stack over 40–50°E/10–20°N and crossed the 45° SLDEM component boundary. Its 1,448-tile hierarchy includes 1,021 level-8 tiles, or 16 reference 64-tile work chunks.
    - The first real attempt durably checkpointed scan, plan, and a clean 27-pack publication, then cooperatively cancelled during full validation with exit 130 and structured terminal evidence. The resumed benchmark fully validated, rebuilt incrementally with 1,448/1,448 reuse, and reproduced database identity `b590ed620d4931a7023a08de5d726d3625685af0a6028dd6b85e315e7cb069a6` plus all 27 ordered pack identities. Qualification records `resume_byte_identical = true`; inspection and all five required exports passed. A downstream empty-prototype selection defect exposed a preserved failed attempt and is fixed by a tested read-only published-cache fallback.

Next action: Pause before Priority 1 as requested. In a future explicitly authorized session, begin with metadata/sampling lifetime separation, the physical-member spatial index, and the bounded decoded-block cache; do not launch the full scale workflow until the Priority-1 resource blockers are removed.
