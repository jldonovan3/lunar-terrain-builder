interface:
  display_name: "Use Subagents"
  short_description: "Dispatch concise, neutral independent agent work"
  default_prompt: "Use $use-subagents to decide whether independent agents add value and dispatch neutral assignments."
